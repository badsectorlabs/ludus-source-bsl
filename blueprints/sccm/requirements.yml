# requirements.yml: pinned collection versions for the SCCM blueprint.

collections:
  # SCCM/MECM hierarchy collection — deploys full CAS + Primary + Secondary site
  # Install manually: ludus ansible collection add https://github.com/Mayyhem/ludus_sccm
  - name: mayyhem.ludus_sccm
    source: https://github.com/Mayyhem/ludus_sccm
    type: git
    version: main
